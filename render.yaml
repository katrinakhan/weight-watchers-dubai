services:
  - type: web
    name: weight-watchers-dubai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 start.py
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: "3.11.9"
